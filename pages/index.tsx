import React from 'react';
import type { NextPage } from 'next';
import { Flex, Text } from '@chakra-ui/react';

const Home: NextPage = () => (
  <Flex>
    <Text>Main</Text>
  </Flex>
  );

export default Home;
